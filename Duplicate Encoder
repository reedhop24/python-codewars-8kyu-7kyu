def duplicate_encode(word):
    new_str = ''
    to_lower = word.lower()
    for x in to_lower:
        if to_lower.count(x) > 1:
            new_str += ')'
        else:
            new_str += '('
    return new_str
